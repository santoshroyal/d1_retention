# Generated reports — kept locally, not in repo
outputs/*
!outputs/.gitkeep

# Auto-fetched sheet cache (managed by tools/_sheet_store.py).
# Only the primary app_health_daily.csv is checked in.
data/sheets/_cache/
data/sheets/app_d1_retention_health_*.csv

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Secrets (in case any future fetcher needs them)
.secrets/
.env

# uv
.python-version

# PM-local notes (commit conventions, session notes) — not for the shared repo
.pm/
